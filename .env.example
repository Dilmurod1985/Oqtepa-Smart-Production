PORT=3000

# Production storage. If this is empty, the app uses local JSON files.
DATABASE_URL=postgres://user:password@localhost:5432/oqtepa

# Disable SSL only for local PostgreSQL if needed.
# PGSSLMODE=disable
